server {
    listen 80;
    listen [::]:80;
    server_name www.eventumcol.com eventumcol.com;
    
    # Redirigir HTTP a HTTPS
    return 301 https://www.eventumcol.com$request_uri;
}

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name www.eventumcol.com eventumcol.com;
    
    # Configuración SSL (ajustar rutas según tu certificado)
    # ssl_certificate /path/to/certificate.crt;
    # ssl_certificate_key /path/to/private.key;
    
    root /var/www/eventumcol.com;
    index index.html;
    
    # PWA: nunca cachear largo el manifiesto del service worker ni el shell HTML
    location = /ngsw.json {
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
        try_files $uri =404;
    }
    location = /ngsw-worker.js {
        add_header Cache-Control "no-cache" always;
        try_files $uri =404;
    }
    location = /index.html {
        add_header Cache-Control "no-cache, no-store, must-revalidate" always;
    }

    # Configuración para SPA - redirigir todas las rutas al index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Cache para assets estáticos (hasheados en build; no aplica a ngsw-worker.js por location = arriba)
    location ~* \.(ico|jpg|jpeg|png|gif|css|js|woff|woff2|ttf|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Compresión GZIP
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
    
    # Seguridad
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;
}

